name: game_metrics_mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  flutter_secure_storage: ^9.2.4
  global_configuration: ^2.0.0
  http: ^1.3.0
  intl: ^0.20.2
  package_info_plus: ^8.3.0
  rename_app: ^1.6.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/cfg/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/logo.png"
